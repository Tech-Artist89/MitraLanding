# =============================================================================
# .env.example - TEMPLATE FÜR UMGEBUNGSVARIABLEN
# =============================================================================
# 
# WICHTIG: Diese Datei enthält KEINE echten Passwörter oder sensible Daten!
# Sie dient als Vorlage und wird ins Git Repository committed.
#
# SETUP:
# 1. Kopieren: cp .env.example .env
# 2. .env mit echten Daten füllen
# 3. .env ist in .gitignore und wird NICHT committed
#
# =============================================================================

# =============================================================================
# SERVER EINSTELLUNGEN (PFLICHT)
# =============================================================================
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:4200

# =============================================================================
# E-MAIL KONFIGURATION (PFLICHT)
# =============================================================================
# SMTP Server Einstellungen - mit Ihren echten Daten ersetzen

SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@example.com
SMTP_PASS=your-password-here
SMTP_FROM=your-email@example.com

# =============================================================================
# FIRMENINFORMATIONEN (PFLICHT)
# =============================================================================
# Diese Daten erscheinen in E-Mails und PDFs

COMPANY_NAME=Your Company Name
COMPANY_EMAIL=info@your-company.com
COMPANY_PHONE=+49 30 12345678
COMPANY_ADDRESS=Your Street 123, 12345 Your City
COMPANY_WEBSITE=www.your-company.com

# =============================================================================
# OPTIONALE EINSTELLUNGEN
# =============================================================================
# Diese können Sie unverändert lassen oder anpassen

LOG_LEVEL=info
DEBUG_EMAIL=false
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=10

# =============================================================================
# ANLEITUNG FÜR VERSCHIEDENE E-MAIL PROVIDER
# =============================================================================

# GMAIL:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-gmail@gmail.com
# SMTP_PASS=your-16-digit-app-password
# SMTP_FROM=your-gmail@gmail.com
# 
# Setup: Google Account → Security → 2-Step Verification → App passwords

# OUTLOOK/HOTMAIL:
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@outlook.com
# SMTP_PASS=your-outlook-password
# SMTP_FROM=your-email@outlook.com

# IONOS (1&1):
# SMTP_HOST=smtp.ionos.de
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@your-domain.com
# SMTP_PASS=your-email-password
# SMTP_FROM=your-email@your-domain.com

# STRATO:
# SMTP_HOST=smtp.strato.de
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=your-email@your-domain.com
# SMTP_PASS=your-email-password
# SMTP_FROM=your-email@your-domain.com